<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
	<version>4.1.0</version>
		<relativePath /> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.example</groupId>
	<artifactId>sec-api</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>SEC API Service</name>
	<description>Spring Boot service for fetching and processing SEC financial data</description>
	<url />
	<licenses>
		<license />
	</licenses>
	<developers>
		<developer />
	</developers>
	<scm>
		<connection />
		<developerConnection />
		<tag />
		<url />
	</scm>
	<properties>
		<java.version>25</java.version>

		<!-- One switch that disables every format/static-analysis gate.
		     The Docker build stage passes -Dquality.skip=true because it copies
		     only pom.xml + src/ (no config/, no .git, no .mvn), so any gate that
		     resolves a config file would fail there. Do not remove without also
		     fixing springboot/Dockerfile. Never pass it locally. -->
		<quality.skip>false</quality.skip>

		<!-- Boot's parent pluginManagement covers only compiler/failsafe/jar/
		     war/resources/shade, so every quality plugin needs an explicit
		     version here. -->
		<enforcer.version>3.6.3</enforcer.version>
		<spotless.version>3.9.0</spotless.version>
		<checkstyle.plugin.version>3.6.0</checkstyle.plugin.version>
		<checkstyle.version>13.9.0</checkstyle.version>

		<!-- Coverage floor enforced by jacoco:check at verify. Actual at the time
		     CI started running verify: 81.5% line, 61.9% branch. Raise this
		     deliberately when coverage improves; never lower it to make a build
		     pass. Overridable on the command line so the gate itself can be
		     tested (-Djacoco.line.minimum=0.95 should fail). -->
		<jacoco.line.minimum>0.76</jacoco.line.minimum>
		<spotbugs.annotations.version>4.10.3</spotbugs.annotations.version>
		<!-- 3.28.0 bundles PMD 7.17.0. Do not drop below it: 3.27.0 shipped PMD
		     7.14.0, which rejects the targetJdk value maven-pmd-plugin derives
		     from java.version above with "Unsupported targetJdk value '25'".
		     Java 25 support landed in PMD 7.16.0. -->
		<pmd.plugin.version>3.28.0</pmd.plugin.version>
		<!-- Error Prone 2.43.0 raised its own floor to JDK 21, and versions
		     before that cannot run on JDK 25 at all, so this property is pinned
		     to java.version above rather than being independently upgradable. -->
		<errorprone.version>2.50.0</errorprone.version>
		<!-- Error Prone check severities.

		     Default tiers: ERROR-tier checks fail the build, WARNING-tier ones are
		     advisory. The tree is clean at ERROR tier, so the gate stays green.

		     The :OFF entries silence pure-style checks that would drown the warnings
		     worth reading. Do not add one without saying why it does not apply.

		     The :ERROR entries promote WARNING-tier checks this repo treats as build
		     failures. On MAIN sources these are belt-and-braces: -Werror (see the
		     compiler plugin below) already fails on any warning. They are kept because
		     they document which findings matter most, they survive -Werror being
		     removed, and errorprone.checks.common also applies to TEST sources, which
		     deliberately do NOT get -Werror. Each was driven to zero before promotion:
		       UnusedMethod/UnusedVariable/UnusedNestedClass - dead code. UnusedVariable
		         is the only analyser here that catches an unused constructor-injected
		         field: PMD counts `this.x = x;` as a use and SpotBugs does not do fields
		         at all, so without it dead Spring dependencies accumulate unseen.
		       JavaTimeDefaultTimeZone - a zone-less LocalDate.now()/Year.now() reads the
		         host default zone, so "today" differs between a laptop, CI and Fargate.
		         That silently shifts trading-day and filing-window boundaries. Pass a
		         MarketTime constant (see util/MarketTime.java) instead. -->
		<errorprone.checks.common>-Xplugin:ErrorProne \
		    -Xep:MissingSummary:OFF \
		    -Xep:NonApiType:OFF \
		    -Xep:InlineFormatString:OFF \
		    -Xep:MixedMutabilityReturnType:OFF \
		    -Xep:ArrayRecordComponent:OFF \
		    -Xep:JavaTimeDefaultTimeZone:ERROR</errorprone.checks.common>
		<errorprone.checks.main>${errorprone.checks.common} \
		    -Xep:UnusedMethod:ERROR \
		    -Xep:UnusedVariable:ERROR \
		    -Xep:UnusedNestedClass:ERROR</errorprone.checks.main>
		<errorprone.checks.test>${errorprone.checks.common}</errorprone.checks.test>

		<!-- AWS SDK v2, used only to publish the weekly adjusted-price validation report to
		     SNS. Managed by the SDK BOM below so the single sns artifact and its transitive
		     core stay on one version. -->
		<awssdk.version>2.50.2</awssdk.version>
	</properties>
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>software.amazon.awssdk</groupId>
				<artifactId>bom</artifactId>
				<version>${awssdk.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>
	<dependencies>
		<!-- Report delivery for the weekly validate-prices task. The task role carries an
		     sns:Publish policy scoped to the report topic; credentials come from the Fargate
		     container role, so no keys are configured anywhere. -->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>sns</artifactId>
		</dependency>
		<!-- @SuppressFBWarnings only; provided+optional so it never ships in the jar -->
		<dependency>
			<groupId>com.github.spotbugs</groupId>
			<artifactId>spotbugs-annotations</artifactId>
			<version>${spotbugs.annotations.version}</version>
			<scope>provided</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<!-- Boot 4 moved Flyway auto-configuration into the per-feature
		     spring-boot-flyway module; bare flyway-core no longer activates it -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.hibernate.orm</groupId>
			<artifactId>hibernate-envers</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Boot 4 ships @WebMvcTest in a per-module test artifact -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Boot 4 ships @DataJpaTest in a per-module test artifact -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-data-jpa-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>
	</dependencies>


	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<version>${enforcer.version}</version>
				<configuration>
					<skip>${quality.skip}</skip>
				</configuration>
				<executions>
					<execution>
						<id>enforce-build-environment</id>
						<phase>validate</phase>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<!-- Cleared by .mvn/wrapper/maven-wrapper.properties and the
								     maven:3.9.16 builder image in Dockerfile -->
								<requireMavenVersion>
									<version>[3.9.0,)</version>
								</requireMavenVersion>
								<!-- Matches <java.version> and the eclipse-temurin:25 runtime
								     image. A lower local JDK would silently produce a jar the
								     container cannot load, so fail at validate instead. -->
								<requireJavaVersion>
									<version>[25,)</version>
								</requireJavaVersion>
								<banDuplicatePomDependencyVersions />
							</rules>
							<fail>true</fail>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
				<version>${spotless.version}</version>
				<!-- Config lives at plugin level, not inside <execution>, because CI
				     invokes `spotless:check` as a standalone goal and standalone goal
				     invocations do not inherit execution-scoped <configuration>. -->
				<configuration>
					<skip>${quality.skip}</skip>
					<java>
						<includes>
							<include>src/main/java/**/*.java</include>
							<include>src/test/java/**/*.java</include>
						</includes>
						<!-- Deliberately NO googleJavaFormat/palantirJavaFormat: this step
						     set never reflows code, so brace placement and line wrapping
						     stay exactly as written. Adopting a real formatter is a
						     separate, whole-tree decision. -->
						<toggleOffOn />
						<removeUnusedImports />
						<importOrder>
							<!-- trailing empty group = everything else; \# = statics last -->
							<order>java,javax,jakarta,org,com,,\#</order>
						</importOrder>
						<trimTrailingWhitespace />
						<endWithNewline />
						<indent>
							<spaces>true</spaces>
							<spacesPerTab>4</spacesPerTab>
						</indent>
					</java>
				</configuration>
				<executions>
					<execution>
						<id>spotless-check</id>
						<phase>validate</phase>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-checkstyle-plugin</artifactId>
				<version>${checkstyle.plugin.version}</version>
				<dependencies>
					<!-- override the older Checkstyle the plugin bundles -->
					<dependency>
						<groupId>com.puppycrawl.tools</groupId>
						<artifactId>checkstyle</artifactId>
						<version>${checkstyle.version}</version>
					</dependency>
				</dependencies>
				<!-- Plugin-level, not execution-level: CI runs `checkstyle:check` as a
				     standalone goal, which does not inherit execution config. -->
				<configuration>
					<skip>${quality.skip}</skip>
					<configLocation>config/checkstyle/checkstyle.xml</configLocation>
					<!-- maven-checkstyle-plugin does not populate ${config_loc} itself;
					     the SuppressionFilter in checkstyle.xml needs it to locate
					     suppressions.xml. -->
					<propertyExpansion>config_loc=${project.basedir}/config/checkstyle</propertyExpansion>
					<includeTestSourceDirectory>true</includeTestSourceDirectory>
					<consoleOutput>true</consoleOutput>
					<failsOnError>true</failsOnError>
					<violationSeverity>error</violationSeverity>
					<linkXRef>false</linkXRef>
				</configuration>
				<executions>
					<execution>
						<id>checkstyle-check</id>
						<phase>validate</phase>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<!-- Run tests from target/ so the optional .env import in
					     application.properties never resolves the real springboot/.env -->
					<workingDirectory>${project.build.directory}</workingDirectory>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.15</version>
				<executions>
					<execution>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>test</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
					<execution>
						<id>check</id>
						<goals>
							<goal>check</goal>
						</goals>
						<configuration>
							<rules>
								<rule>
									<element>BUNDLE</element>
									<limits>
										<limit>
											<counter>LINE</counter>
											<value>COVEREDRATIO</value>
											<minimum>${jacoco.line.minimum}</minimum>
										</limit>
									</limits>
								</rule>
							</rules>
							<excludes>
								<exclude>com/fattorestreet/sec_api/SecApiApplication.class</exclude>
							</excludes>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-pmd-plugin</artifactId>
				<version>${pmd.plugin.version}</version>
				<configuration>
					<skip>${quality.skip}</skip>
					<rulesets>
						<ruleset>config/pmd/ruleset.xml</ruleset>
					</rulesets>
					<includeTests>false</includeTests>
					<printFailingErrors>true</printFailingErrors>
					<failOnViolation>true</failOnViolation>
					<linkXRef>false</linkXRef>
				</configuration>
				<executions>
					<execution>
						<id>pmd-check</id>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>com.github.spotbugs</groupId>
				<artifactId>spotbugs-maven-plugin</artifactId>
				<version>4.10.3.0</version>
				<!-- Plugin-level so `spotbugs:check` works as a standalone goal too. -->
				<configuration>
					<skip>${quality.skip}</skip>
					<effort>Max</effort>
					<!-- Kept at Low deliberately. Raising to Medium would silence
					     FindSecBugs patterns that report at Low while NOT silencing
					     EI_EXPOSE_REP, which is already Medium, so the threshold is
					     the wrong instrument. The exclude filter is the right one. -->
					<threshold>Low</threshold>
					<xmlOutput>true</xmlOutput>
					<htmlOutput>true</htmlOutput>
					<includeTests>false</includeTests>
					<failOnError>true</failOnError>
					<excludeFilterFile>config/spotbugs/exclude.xml</excludeFilterFile>
					<plugins>
						<plugin>
							<groupId>com.h3xstream.findsecbugs</groupId>
							<artifactId>findsecbugs-plugin</artifactId>
							<version>1.14.0</version>
						</plugin>
					</plugins>
				</configuration>
				<executions>
					<execution>
						<id>spotbugs-check</id>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!-- Error Prone runs as a javac plugin during `compile`, which the Docker
		     build stage reaches via `package`. Unlike the other gates it cannot be
		     turned off with a <skip> parameter, so it lives in a profile that
		     deactivates whenever -Dquality.skip is passed on the command line.
		     Maven property activation inspects command-line -D and settings.xml,
		     not the <properties> block, so the default quality.skip=false above
		     does NOT deactivate it for a normal build. -->
		<profile>
			<id>errorprone</id>
			<activation>
				<property>
					<name>!quality.skip</name>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-compiler-plugin</artifactId>
						<configuration>
							<compilerArgs>
								<arg>-XDcompilePolicy=simple</arg>
								<arg>--should-stop=ifError=FLOW</arg>
								<!-- Main sources are warning-free and -Werror keeps them that way:
								     ANY javac or Error Prone warning fails the build, so a new
								     WARNING-tier finding cannot accumulate unnoticed and no
								     per-check promotion list has to be maintained.
								     -Xlint:deprecation is enabled deliberately; rawtypes/unchecked
								     are NOT, since Spring and JPA generics would make them noise.
								     If this ever has to come off, promote the checks that matter
								     individually in errorprone.checks.main instead. -->
								<arg>-Xlint:deprecation</arg>
								<arg>-Werror</arg>
								<!-- See the errorprone.checks.* properties for what is turned off,
								     what is promoted, and why. -->
								<arg>${errorprone.checks.main}</arg>
							</compilerArgs>
							<annotationProcessorPaths>
								<path>
									<groupId>com.google.errorprone</groupId>
									<artifactId>error_prone_core</artifactId>
									<version>${errorprone.version}</version>
								</path>
							</annotationProcessorPaths>
						</configuration>
						<executions>
							<!-- Test sources get the same checks EXCEPT the dead-code trio, which
							     is left advisory there. Mockito's @InjectMocks is fed by @Mock
							     fields that are never read when a test neither stubs nor verifies
							     them: load-bearing (dropping one injects null) but indistinguishable
							     from dead code to Error Prone, which cannot see field injection.
							     Promoting UnusedVariable on tests would fail the build for a
							     correct and common test idiom. JavaTimeDefaultTimeZone stays at
							     ERROR here, since a zone-less now() makes a test depend on the
							     machine's clock zone just as much as production code does. -->
							<execution>
								<id>default-testCompile</id>
								<goals>
									<goal>testCompile</goal>
								</goals>
								<configuration>
									<compilerArgs>
										<arg>-XDcompilePolicy=simple</arg>
										<arg>--should-stop=ifError=FLOW</arg>
										<arg>${errorprone.checks.test}</arg>
									</compilerArgs>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>