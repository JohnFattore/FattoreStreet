# React App CLAUDE.md

This file provides guidance to Claude Code when working inside `react-app/`.

Conventions are covered by the path-scoped rule `.claude/rules/react-typescript.md`, auto-loaded when working with `react-app/**/*.{ts,tsx}`.

## Writing Tests

@../.claude/skills/react-tests/SKILL.md
