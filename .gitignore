django/mysite/.env
.env
**/__pycache__/
**/.DS_Store

venv/
.venv/
env/
.env/

*.log
deploy.sh
# the automated deploy script is secret-free (config comes from Secrets
# Manager via SECRETS_ARN) and must be versioned -- CI deploys it by SHA
!deploy/deploy.sh
django/restaurants/yelp_dataset

react-app/node_modules
react-app/dist
.env.*
!deploy/.env.example
!react-app/.env.compose
# public URLs only (no secrets); committed so the hermetic nginx image build
# produces the real production bundle from a clean checkout
!react-app/.env.production
react-app/coverage
react-app/.sass-cache
react-app/src/styles/custom.css
react-app/src/styles/custom.css.map


django/chatbot/secret.txt

kubernetes/

nginx/static/
django/static/
nginx/dist/

springboot/data/

# Terraform — keep .tfvars.example tracked, ignore real vars + state (contain secrets)
*.tfvars
*.tfstate
*.tfstate.*
.terraform/
# Saved plan files embed the resolved variable values, so treat them like tfvars.
tfplan
tfplan-*
crash.log

llm/llama.cpp/
llm/stable-diffusion.cpp/
llm/models/
llm/output/
llm/input/

llm/machineLearning/yelp_dataset
llm/machineLearning/fred
llm/machineLearning/huggingFace.txt

llm

daily_prices_data.sql

SECRETS.md