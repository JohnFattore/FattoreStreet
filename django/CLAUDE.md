# Django CLAUDE.md

This file provides guidance to Claude Code when working inside `django/`.

Conventions are covered by the path-scoped rule `.claude/rules/django-drf.md`, auto-loaded when working with `django/**/*.py`.

## Writing Tests

@../.claude/skills/django-tests/SKILL.md
